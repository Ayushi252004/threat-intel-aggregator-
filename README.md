# threat-intel-aggregator-
A rule-based Python tool that collects IOCs (IPs, domains, URLs, hashes) from CSV/TXT/JSON threat feeds, normalizes them, correlates repeats across sources to assign severity ratings, and exports deployment-ready blocklists plus a summary intelligence report — built for practical SOC/blue-team workflows.
